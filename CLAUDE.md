# CLAUDE.md

Всегда общайся с пользователем на русском языке

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty aside from a PhpStorm/IntelliJ `.idea` project configuration. The `.idea` module file (`legalacts.opengov.kz.iml`) is configured as a Python module targeting Python 3.8 (`~\AppData\Local\Programs\Python\Python38\python.exe`), but no source code, dependency manifest (e.g. `requirements.txt`, `pyproject.toml`), or README exists yet.

There are no build, lint, or test commands to document because no code, framework, or tooling has been added to the project.

## Next steps for future instances

Once source code is added to this repository, update this file with:
- Commands to install dependencies, run the app, lint, and run tests (including how to run a single test)
- The high-level architecture once a project structure exists
